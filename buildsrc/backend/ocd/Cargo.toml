[package]
name = "ocd"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
nix = { version = "0.30", features = ["mount", "sched"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
